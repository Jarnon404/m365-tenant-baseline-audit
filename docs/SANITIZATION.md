# Sanitization

The default privacy mode is `Sanitized`.

Sanitized mode masks:

- GUID-like tenant identifiers
- e-mail addresses
- tenant `.onmicrosoft.com` names

Generated reports must still be reviewed before sharing. Sanitization is a helper, not a legal guarantee delivered by a magical compliance fairy.
